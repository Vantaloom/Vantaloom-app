# XcodeGen 生成的工程与构建产物
*.xcodeproj
*.xcworkspace
build/
DerivedData/
xcuserdata/
# CI 把公开仓根的 web/ 同步到这里；私仓里前端在 apps/vantaloom/out。
# 必须锚定到本目录根（/web/）：不锚定的 web/ 在 Windows/macOS 的大小写不敏感
# 匹配下会吞掉别的同名目录（Swift 源目录因此也不叫 Web，见 project.yml 头注释）。
/web/
# 操控端节点 xcframework 见 node/.gitignore
