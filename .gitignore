# Rust build output
/src-tauri/target/

# Tauri-generated Android/Apple projects (regenerated by `tauri android init`
# in CI each run). When we start committing custom manifest / foreground-service
# changes, un-ignore src-tauri/gen/android and stop re-initialising in CI.
/src-tauri/gen/

# Signing material — never commit
*.keystore
*.jks
key.properties

node_modules/
dist/
