# The compiled Next.js web export is copied here by CI (build-apk.yml) from the
# repo web/ dir, then bundled and served over http://vantaloom.localhost by
# WebViewAssetLoader. Kept out of git (see root .gitignore).
